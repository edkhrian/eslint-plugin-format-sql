import {
  sql,
  postgresql,
  db2,
  hive,
  mariadb,
  mysql,
  n1ql,
  plsql,
  bigquery,
  redshift,
  singlestoredb,
  spark,
  sqlite,
  snowflake,
  transactsql,
  trino,
  SqlLanguage,
  DialectOptions,
} from 'sql-formatter';

export const dialects: { [key in SqlLanguage]: DialectOptions } = {
  sql,
  postgresql,
  db2,
  hive,
  mariadb,
  mysql,
  n1ql,
  plsql,
  bigquery,
  redshift,
  singlestoredb,
  spark,
  sqlite,
  snowflake,
  transactsql,
  trino,
};
